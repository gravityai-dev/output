// Import shared credentials from plugin-base
// Output nodes typically don't require credentials, but this is required by spec

// Re-export for this package (empty for now as output nodes don't use credentials)
export {};
